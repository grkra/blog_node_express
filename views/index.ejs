<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles/main.css">
    <title>Blog</title>
</head>

<body class="bg-body-secondary">
    <header class="container">
        <div class="d-flex justify-content-end py-3">
            <ul class="nav nav-pills">
                <li class="nav-item"><a href="/" class="nav-link active" aria-current="page">Home</a></li>
                <li class="nav-item"><a href="/add" class="nav-link">Dodaj nowy wpis</a></li>
            </ul>
        </div>
    </header>
    <main>
        <div class="container">
            <div class="p-4 p-md-5 border rounded-3 bg-body">
                <div class="row">
                    <div id="postList" class="col-2">
                        <p> Przeglądaj wpisy </p>
                        <ul>
                            <% posts.forEach(post=> { %>

                                <li><a href="/post/<%= post.id %>">
                                        <%= post.title %>
                                    </a></li>
                                <% }); %>
                        </ul>
                    </div>
                    <div id="post" class="col">
                        <% if(locals.post) { %>
                            <div>
                                <article class="blog-post">
                                    <h1>
                                        <%= post.title %>
                                    </h1>
                                    <p>
                                        <%= post.text %>
                                    </p>
                                </article>
                            </div>
                            <div class="text-end">
                                <a href="/delete/<%= post.id %>" class="btn btn-outline-danger me-3"> Usuń post
                                </a>

                                <a href="/edit/<%= post.id %>" class="btn btn-outline-primary"> Edytuj
                                </a>
                            </div>
                            <% } else { %>
                                <%- include("partials/formAddPost.ejs") %>
                                    <% } %>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>

</html>